job:
  KIND: TrainingJob
  inputs:
    KIND: ParquetReader
    path: "${tests_path:}/data/inputs.parquet"
    limit: 1500
  targets:
    KIND: ParquetReader
    path: "${tests_path:}/data/targets.parquet"
    limit: 1500
